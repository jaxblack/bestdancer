# TODO / 路线图 · 本周热舞

> 愿景：成为以统一策展标准产出的**全平台热舞榜单**。每个平台保留其原始热度和链接，通过关键词、发布时间、内容类型与人工细筛形成当周跨平台候选池，而不是将不可比的热度数值直接相加。
> 现状：2026-W29 已跑通本地周工作区、抖音关注页发现/人工下载、Instagram/TikTok 关键词候选发现、人工细筛、音画同步渲染、真实作者署名、字幕+片尾。详见 [docs/skills/douyin-weekly-pipeline.md](docs/skills/douyin-weekly-pipeline.md) 和 [docs/skills/QUICKSTART.md](docs/skills/QUICKSTART.md)。
> 下面是后续优化项，按优先级大致排列，慢慢迭代。

## 🔥 用户明确要的下轮改进（2026-W29 反馈）

- [ ] **字幕淡出** —— 不全程常驻，说完/一定时间后消失（render_demo.py 里给 `vo_caption` 加淡入淡出遮罩，参考现有 fade 逻辑）
- [ ] **显示舞蹈/曲名** —— 从 `aweme_detail.desc` 或 `aweme_detail.music.title` 提取，加到字幕
- [ ] **难度配置后台** —— 现在星级写死 3.0，需要一个 UI/CLI 让人手动调每段舞的 star/tempo/complexity 等
- [x] **发现规则后台** —— 每期选择平台、关键词、发布时间、最低点赞、仅视频与候选上限；可切换最近 $n$ 周工作区。
- [x] **跨平台候选入口（初版）** —— 抖音关注页；Instagram/TikTok 关键词搜索；TikTok 原生「视频」分类后按发布时间与点赞数筛选。
- [ ] **小红书公开搜索筛选** —— 接入公开站登录态后操作原生「筛选」面板，支持视频、最近一周与热度排序。
- [ ] **平台热度归一化** —— 定义平台内相对排名、发布时间衰减与人工编辑权重，避免把不同平台的绝对点赞数直接比较。
- [ ] **跨平台去重** —— 同舞蹈、同编舞、转载和镜像视频合并为一个候选实体，保留全部来源链接。

## 🎯 正在酝酿：全平台自动化采集（OpenClaw / Hermes）

目标：每天定时跑，自动产出跨平台候选，减少人工找片；每个平台优先使用其公开页面或真实客户端允许的筛选能力，下载始终在人工细筛之后执行。

- [x] **关键词驱动搜索（初版）**：后台维护关键词清单；Instagram/TikTok 已接入，抖音从关注页发现，小红书待公开站登录态稳定后接入。
- [x] **按平台条件粗筛（初版）**：TikTok 支持原生视频分类，并按最近一周、点赞门槛和热度降序输出候选。
- [ ] **平台适配器**：为小红书、Instagram、Bilibili、YouTube 补齐各自的结果解析、原生筛选操作和失败诊断。
- [ ] **真机下载**：用 OpenClaw / Hermes 驱动允许的平台客户端 UI，仅下载人工选中的视频。
- [ ] **落地入库**：下载后按 `<id>__<source>__<slug>.mp4` 命名丢进 `assets/incoming/<week>/`，自动触发 `pipeline/intake.py`。
- [ ] **去重**：记录历史已推荐过的舞（指纹/URL/标题），避免同一支重复上榜。
- [ ] **调度**：OpenClaw / Hermes 定时任务（每天定时搜索 + 每周汇总成一期）。
- [ ] **登录态**：为每个平台维护本机浏览器/客户端登录检查与失效提示；不在仓库保存 Cookie、令牌或会话文件。
- [x] **合法真片源**：`pipeline/fetch_stock.py` — 默认 Mixkit(免密钥、许可可用)，可选 Pexels(需免费 key，竖版更好)；render 自动当背景。与真机抖音下载二选一或并用。

> 关键取舍：真机 + 真客户端最稳（反爬友好），难点在 UI 自动化稳定性与登录态维护。

## 🛠 待接入（把文本流程跑成成片）

- [x] **合成脚本(demo版)**：`pipeline/render_demo.py` — 竖版 9:16、真片当背景、星级/动作/字幕、edge-tts 晓伊配音 + 自制 BGM(结尾纯 BGM)，60 秒内。待办：转场动效、真片精剪、BGM 侧链闪避。
- [x] **BGM**：`pipeline/make_bgm.py` — numpy 纯合成轻快节拍(零版权)，可 `--bpm` 调。
- [x] **TTS 配音**：`edge-tts` 神经女声(默认 `zh-CN-XiaoxiaoNeural`；`zh-CN-XiaoyiNeural` 更活泼)，SAPI 离线回退。
- [ ] **片头/片尾固定素材**：用 prompt 05 + 已定配色（`#0B0B14` / `#FF2E9A` / `#16E0FF` / `#FFE14D`）生成一次，长期复用。
- [ ] **封面模板**：小红书封面 main/sub 套版（品红描边白字）。

## 📋 Backlog

- [ ] `classics_pool` 旧舞库维护（经典回归的选池）。
- [ ] 合规：水印保留检查、`{credits}` 署名回填、BGM 版权替换策略、下架响应流程。
- [ ] 发布：抖音 / 小红书 发布（手动 → 半自动）。
- [ ] 数据回流：发布后回收点赞/完播，反哺下期选题权重。

## 📣 简介 / 运营固定项

- [x] **简介固定尾部 `{FOOTER}`**：接收投稿 · 北京随舞宣传报名 · 品牌合作 · 如有侵权请联系删除（已进 prompts/00 + 04）。
- [ ] **投稿渠道**：定邮箱 / 表单 / 私信话术，填 `{contact}`。
- [ ] **北京随舞宣传报名**：落地页 / 报名表 + 审核流程。
- [ ] **品牌合作**：报价卡 / 联系方式 / 案例位。
- [ ] **侵权处理 SOP**：收到通知即撤 + 台账记录。
- [ ] **署名 `{credits}` 回填**：每期把上榜编舞者与原链接填进简介。
- [x] **个人品牌 / 水印**：圆形头像 logo + `BestDancer` 水印已上（`brand/logo.png` 占位，由个人照裁 1:1）。待做：真 logo / 卡通形象、统一字体。BestDancer 为独立品牌，不与参考站杂系。

## ❓ 开放问题

- 每期时长：抖音偏爱 15–60s，小红书可 1–3min → 是否出「抖音精简版 + 小红书完整版」两切？
- 难度 rubric 的 `move_notes` 由谁产：人工看一遍，还是接视觉模型自动描述动作？
- 主色是否需要季度换色（夏/冬主题）？
